% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_exponential.R
\name{simulate_exponential}
\alias{simulate_exponential}
\title{Simulate Dose-Response Data from an Exponential Model}
\usage{
simulate_exponential(
  dose_levels,
  n_per_dose,
  e0,
  e1,
  k,
  sigma,
  dist = "normal",
  df = 3,
  seed = NULL
)
}
\arguments{
\item{dose_levels}{Numeric vector of dose levels.}

\item{n_per_dose}{Either a single integer (equal sample size per dose) or a vector matching `dose_levels`.}

\item{e0}{Baseline effect. Can be a scalar or a vector.}

\item{e1}{Maximum effect.}

\item{k}{Rate constant (controls curve steepness).}

\item{sigma}{Standard deviation for residual error.}

\item{dist}{Residual error distribution: `"normal"` (default), `"lognormal"`, `"uniform"`, `"t"`, or `"gamma"`.}

\item{df}{Degrees of freedom for t-distribution.}

\item{seed}{Optional seed for reproducibility.}
}
\value{
Data frame with columns: `id`, `dose`, `pred`, `ipred`, `scenario`.
}
\description{
Simulates data from an exponential response model:
\deqn{pred = e0 + e1 * (1 - exp(-k * dose))}
}
\details{
All combinations of `e0`, `e1`, and `k` will be simulated.
}
\examples{
simulate_exponential(
  dose_levels = c(0, 1, 5, 10),
  n_per_dose = 10,
  e0 = 0, e1 = 1, k = 0.2,
  sigma = 0.2,
  dist = "normal",
  seed = 200
)

}
