% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_loglinear.R
\name{simulate_loglinear}
\alias{simulate_loglinear}
\title{Simulate Dose-Response Data from a Log-Linear Model}
\usage{
simulate_loglinear(
  dose_levels,
  n_per_dose,
  e0,
  e1,
  log_shift = 1,
  sigma,
  dist = "normal",
  df = 3,
  seed = NULL
)
}
\arguments{
\item{dose_levels}{Numeric vector of dose levels.}

\item{n_per_dose}{Either a single integer (equal sample size per dose) or a vector matching `dose_levels`.}

\item{e0}{Baseline effect.}

\item{e1}{Slope of the log-linear relationship.}

\item{log_shift}{Value added inside the log to avoid log(0). Default is 1.}

\item{sigma}{Standard deviation for residual error.}

\item{dist}{Residual error distribution: `"normal"` (default), `"lognormal"`, `"uniform"`, `"t"`, or `"gamma"`.}

\item{df}{Degrees of freedom for t-distribution.}

\item{seed}{Optional seed for reproducibility.}
}
\value{
Data frame with columns: `id`, `dose`, `pred`, `ipred`.
}
\description{
Simulates from a log-linear dose-response model:
\deqn{pred = e0 + e1 * log(dose + log_shift)}
}
\examples{
simulate_loglinear(
  dose_levels = c(0, 1, 5, 10),
  n_per_dose = 10,
  e0 = 0, e1 = 1.5, log_shift = 1,
  sigma = 0.2,
  dist = "normal",
  seed = 100
)

}
