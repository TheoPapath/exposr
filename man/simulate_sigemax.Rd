% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_sigemax.R
\name{simulate_sigemax}
\alias{simulate_sigemax}
\title{Simulate Dose-Response Data from a Sigmoid Emax Model}
\usage{
simulate_sigemax(
  dose_levels,
  n_per_dose,
  e0,
  emax,
  ed50,
  h,
  sigma,
  dist = "normal",
  df = 3,
  seed = NULL
)
}
\arguments{
\item{dose_levels}{Numeric vector of dose levels.}

\item{n_per_dose}{Either a single integer (equal sample size per dose) or a vector matching `dose_levels`.}

\item{e0}{Baseline effect.}

\item{emax}{Maximum drug effect.}

\item{ed50}{Dose producing 50% of `emax`.}

\item{h}{Hill coefficient controlling the curve steepness.}

\item{sigma}{Standard deviation for residual error.}

\item{dist}{Residual error distribution: `"normal"` (default), `"lognormal"`, `"uniform"`, `"t"`, or `"gamma"`.}

\item{df}{Degrees of freedom for t-distribution.}

\item{seed}{Optional seed for reproducibility.}
}
\value{
Data frame with columns: `id`, `dose`, `pred`, `ipred`.
}
\description{
Simulates data from a sigmoid Emax model with Hill coefficient:
\deqn{pred = e0 + emax * dose^h / (ed50^h + dose^h)}
}
\examples{
simulate_sigemax(
  dose_levels = c(0, 1, 5, 10),
  n_per_dose = 10,
  e0 = 0, emax = 1, ed50 = 5, h = 2,
  sigma = 0.2,
  dist = "normal",
  seed = 42
)

}
