% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_dose_response.R
\name{plot_dose_response}
\alias{plot_dose_response}
\title{Plot Dose-Response Relationship}
\usage{
plot_dose_response(
  data,
  dose_var,
  response_var,
  group_var = NULL,
  connect = TRUE,
  fit_model = TRUE,
  show_ci = TRUE,
  return_fit = FALSE,
  color_palette = NULL,
  x_lab = NULL,
  y_lab = NULL,
  log_scale = FALSE,
  title = "Dose-Response Curve"
)
}
\arguments{
\item{data}{A data frame containing the dose and response values.}

\item{dose_var}{Name of the column representing the dose variable.}

\item{response_var}{Name of the column representing the response variable.}

\item{group_var}{Optional; name of grouping variable for stratified plotting.}

\item{connect}{Logical; whether to connect mean points by line (default = TRUE).}

\item{fit_model}{Logical; whether to overlay a logistic/Emax model fit (default = TRUE).}

\item{show_ci}{Logical; whether to include a confidence interval around the model fit (default = TRUE).}

\item{return_fit}{Logical; if TRUE, return the fitted model as an attribute (default = FALSE).}

\item{color_palette}{Optional vector of colors to apply per group.}

\item{x_lab}{Optional x-axis label.}

\item{y_lab}{Optional y-axis label.}

\item{log_scale}{Logical; whether to log-transform x-axis (default = FALSE).}

\item{title}{Optional title.}
}
\value{
A ggplot object (with optional model fit attached as attribute).
}
\description{
Visualizes the dose-response relationship with optional model fits and confidence intervals.
Can be used for continuous endpoints or binary outcomes (0/1).
}
\examples{
# Continuous response
set.seed(123)
df <- data.frame(
  dose = rep(c(0, 5, 10, 20, 40, 80), each = 20),
  response = c(
    rnorm(20, 10, 3),
    rnorm(20, 20, 3),
    rnorm(20, 30, 3),
    rnorm(20, 45, 3),
    rnorm(20, 55, 3),
    rnorm(20, 60, 3)
  )
)
plot_dose_response(df, dose_var = "dose", response_var = "response")

# Binary response with CI and binomial error bars
set.seed(321)
df_bin <- data.frame(
  dose = rep(c(0, 5, 10, 20, 40, 80), each = 50)
)
df_bin$response <- rbinom(nrow(df_bin), 1, plogis((df_bin$dose - 20) / 10))
plot_dose_response(df_bin, dose_var = "dose", response_var = "response")
}
