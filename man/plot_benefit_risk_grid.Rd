% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_benefit_risk_grid.R
\name{plot_benefit_risk_grid}
\alias{plot_benefit_risk_grid}
\title{Plot Benefit-Risk Grid}
\usage{
plot_benefit_risk_grid(
  data,
  efficacy_var,
  safety_var,
  dose_var = NULL,
  efficacy_thresh,
  safety_thresh,
  label_points = TRUE,
  facet_var = NULL,
  efficacy_lower = NULL,
  efficacy_upper = NULL,
  safety_lower = NULL,
  safety_upper = NULL,
  quadrant_colors = list(green = "#ccebc5", red = "#fbb4ae", amber_top = "#ffffcc",
    amber_right = "#d9f0ff"),
  title = "Benefit–Risk Grid",
  x_lab = "Efficacy Probability",
  y_lab = "Safety Risk Probability"
)
}
\arguments{
\item{data}{A data frame containing one row per dose or scenario.}

\item{efficacy_var}{Column name for efficacy estimate or probability.}

\item{safety_var}{Column name for safety estimate or probability.}

\item{dose_var}{Optional column name for labeling dose levels.}

\item{efficacy_thresh}{Numeric; minimum acceptable efficacy.}

\item{safety_thresh}{Numeric; maximum acceptable safety risk.}

\item{label_points}{Logical; if TRUE, label points with dose.}

\item{facet_var}{Optional variable to facet the grid by subgroup.}

\item{efficacy_lower}{Optional column for lower bound of efficacy CI.}

\item{efficacy_upper}{Optional column for upper bound of efficacy CI.}

\item{safety_lower}{Optional column for lower bound of safety CI.}

\item{safety_upper}{Optional column for upper bound of safety CI.}

\item{quadrant_colors}{Named list of fill colors for each benefit-risk quadrant.}

\item{title}{Title for the plot.}

\item{x_lab}{Label for the x-axis.}

\item{y_lab}{Label for the y-axis.}
}
\value{
A ggplot object.
}
\description{
Creates a 2D decision space plot showing efficacy vs safety probabilities or values.
Useful for dose selection or benefit-risk communication. Accepts group-wise data
and overlays thresholds to define acceptable decision regions.
}
\examples{
df <- data.frame(
  dose = c(10, 20, 40, 80, 160),
  prob_efficacy = c(0.2, 0.4, 0.6, 0.8, 0.9),
  prob_safety = c(0.05, 0.1, 0.2, 0.35, 0.6),
  subgroup = rep(c("A", "B"), length.out = 5),
  eff_lo = c(0.1, 0.3, 0.5, 0.7, 0.8),
  eff_hi = c(0.3, 0.5, 0.7, 0.9, 0.95),
  saf_lo = c(0.01, 0.05, 0.1, 0.25, 0.5),
  saf_hi = c(0.08, 0.12, 0.3, 0.45, 0.7)
)
plot_benefit_risk_grid(
  df,
  efficacy_var = "prob_efficacy",
  safety_var = "prob_safety",
  dose_var = "dose",
  efficacy_thresh = 0.6,
  safety_thresh = 0.3,
  efficacy_lower = "eff_lo",
  efficacy_upper = "eff_hi",
  safety_lower = "saf_lo",
  safety_upper = "saf_hi",
  facet_var = "subgroup"
)
df <- data.frame(
  dose = c(10, 20, 40, 80, 160),
  prob_efficacy = c(0.2, 0.4, 0.6, 0.8, 0.9),
  prob_safety = c(0.05, 0.1, 0.2, 0.35, 0.6)
)
plot_benefit_risk_grid(
  df,
  efficacy_var = "prob_efficacy",
  safety_var = "prob_safety",
  dose_var = "dose",
  efficacy_thresh = 0.6,
  safety_thresh = 0.3
)
}
