% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kmplot.R
\name{kmplot}
\alias{kmplot}
\title{Kaplan-Meier Plot for Continuous or Categorical Covariates}
\usage{
kmplot(
  data,
  time_var,
  event_var,
  covariate,
  covariate_type = c("continuous", "categorical"),
  covariate_labels = NULL,
  covariate_label = NULL,
  time_label = NULL,
  survival_label = NULL,
  colors = NULL,
  time_break = 1,
  plot_ci = FALSE,
  show_pval = FALSE,
  show_fit = TRUE,
  cumulative = FALSE,
  return_plot = TRUE,
  show_summary_box = TRUE,
  summary_box_position = c("topright", "topleft", "bottomright", "bottomleft")
)
}
\arguments{
\item{data}{A data.frame containing the input data.}

\item{time_var}{Character. Name of the time-to-event variable.}

\item{event_var}{Character. Name of the event indicator variable (0 = censored, 1 = event).}

\item{covariate}{Character. Name of the covariate to stratify by.}

\item{covariate_type}{Character. One of "continuous" or "categorical".}

\item{covariate_labels}{Optional named vector to rename levels of the covariate.}

\item{covariate_label}{Optional character. Label for the covariate (used in legend and risk table).}

\item{time_label}{Optional character. Label for the x-axis.}

\item{survival_label}{Optional character. Label for the y-axis.}

\item{colors}{Optional character vector of colors for the groups.}

\item{time_break}{Numeric. Interval for x-axis breaks (default is 1).}

\item{plot_ci}{Logical. Whether to show confidence intervals (default = FALSE).}

\item{show_pval}{Logical. Whether to display the p-value (default = FALSE).}

\item{show_fit}{Logical. Whether to print the survfit object (default = TRUE).}

\item{cumulative}{Logical. Whether to plot cumulative incidence instead of survival (default = FALSE).}

\item{return_plot}{Logical. Whether to return the ggplot object (default = TRUE).}
}
\value{
A Kaplan-Meier plot (ggplot2 object) with optional risk table.
}
\description{
This function generates a Kaplan-Meier plot for time-to-event data, stratified
either by quartiles of a continuous covariate or by levels of a categorical covariate.
}
\examples{
library(survival)
library(survminer)
data(lung)

# Example 1: Categorical covariate (sex)
kmplot(data = lung,
time_var = "time",
    event_var = "status",
    covariate = "sex",
    covariate_type = "categorical",
    covariate_labels = c("1" = "Male", "2" = "Female"),
    covariate_label = "Sex",
    time_label = "Time (days)",
    survival_label = "Survival Probability",
    time_break = 100,
    show_pval = TRUE)
# Example 2: Continuous covariate (age)
kmplot(data = lung,
    time_var = "time",
    event_var = "status",
    covariate = "age",
    covariate_type = "continuous",
    covariate_label = "Age Quartiles",
    time_label = "Time (days)",
    survival_label = "Survival Probability",
    time_break = 100,
    show_pval = TRUE)
}
